server:
  port: 8044

eureka:
  instance:
    prefer-ip-address: true
    lease-expiration-duration-in-seconds: 15
    lease-renewal-interval-in-seconds: 5
    hostname: ${spring.application.name}
  client:
    registry-fetch-interval-seconds: 5
    service-url:
<<<<<<< HEAD
      defaultZone: http://cat:cat@xinlangxfpt.mynatapp.cc:8761/eureka/
=======
      defaultZone: http://cat:cat@192.168.5.3:8761/eureka/
>>>>>>> 35afc02f949ad25f2f328e4f4feec3c45b6fa34c

spring:
  application:
    name: project-report
  profiles:
    active: dev
  cloud:
      config:
        fail-fast: true
        name: ${spring.application.name}
        profile: ${spring.profiles.active}
        discovery:
          enabled: true
          service-id: cat-configuration
  sleuth:
    sampler:
      probability: 1.0

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always