server:
  port: 8001

eureka:
  instance:
    prefer-ip-address: true
    lease-expiration-duration-in-seconds: 15
    lease-renewal-interval-in-seconds: 5
    hostname: ${spring.application.name}
  client:
    registry-fetch-interval-seconds: 5
    service-url:


<<<<<<< HEAD
      defaultZone: http://cat:cat@192.168.5.64:8761/eureka/
=======
      defaultZone: http://cat:cat@192.168.5.24:8761/eureka/
>>>>>>> 0bdab14c8ff5f414e64b8b86a9954cd585962f27


spring:
  application:
    name: project-user
  profiles:
    active: dev
  cloud:
      config:
        fail-fast: true
        name: ${spring.application.name}
        profile: ${spring.profiles.active}
        discovery:
          enabled: true
          service-id: cat-configuration
  sleuth:
    sampler:
      probability: 1.0

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always